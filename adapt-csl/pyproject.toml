[tool.ruff]
target-version = "py310"
line-length = 88
exclude = ["adapt_csl_results"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
